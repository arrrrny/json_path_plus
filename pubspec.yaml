name: json_path_plus
description: A Dart port of JSONPath-Plus — full-featured JSONPath query engine with filter expressions, type operators, slices, and safe evaluation.
version: 1.0.0
homepage: https://github.com/JSONPath-Plus/JSONPath

environment:
  sdk: ">=3.0.0 <4.0.0"

dev_dependencies:
  test: ^1.25.0
